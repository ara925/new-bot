// Frontend contexts exports
// This file re-exports context providers and hooks

// Auth context
export { AuthProvider, useAuth } from './AuthContext';

// UI context (can be implemented as needed)
export const createUIContext = () => {
  // Implementation to be added
  return {
    /* UI context implementation */
  };
};

// Credits context (can be implemented as needed)
export const createCreditsContext = () => {
  // Implementation to be added
  return {
    /* Credits context implementation */
  };
};
